# M9-003 final closure boundary

Do not alter M9-001 or M9-002.  M9-003 must:

1. identify `JH-20260813-M9-003` as the spec's `freeze_task`;
2. use a real guard, not a literal demo, for structured key/value and prose
   candidate/read/route/Gate success claims; and
3. parse or otherwise bound the `## 11. Output schema` block so required
   schema entries are checked there, not anywhere in the document.

All existing measurement-only exclusions, XLOAD `UNVERIFIED` boundary, rd/vts
formula, legacy M6 boundary, and applicable-observable convergence semantics
remain mandatory.
