# R2a coupling 代表点可视化

这里选用 accepted coupling route 的 `K=.80` representative 四 case，直接读取原始 `raw/diff-a010-b007-k080`。

图形保留 B_TRIG、B_OUT、secondary、damping、SL/N6 和 storage guards。它用于展示 coupling diagnostic，不替代 R2-A 全矩阵 report，也不把 B_OUT activity当作 event。
