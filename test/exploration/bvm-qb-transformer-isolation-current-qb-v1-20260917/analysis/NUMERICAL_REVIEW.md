# Numerical review — mechanical only

- M is derived exactly as K*sqrt(L_PRI*L_SEC); |K|<1 and the two-winding matrix audit are recorded.
- All source comparisons and integrations use actual stored timestamps; no interpolation/resampling.
- P(...) is raw radians; turns are independent rad/(2*pi) navigation only.
- Power records are descriptive element-oriented traces, not mechanism or hardware claims.
