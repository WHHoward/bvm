# Reference Provenance（W5B）— 指针/索引

> **状态（2026-08-13，M11B-003）**：本文件是**指针/索引**，不是独立参数登记表。
> canonical provenance registry 是
> [SCIENTIFIC_RECONSTRUCTION_OBJECT_MATRIX_V1.yaml](SCIENTIFIC_RECONSTRUCTION_OBJECT_MATRIX_V1.yaml)
> （freeze task `JH-20260813-M11B-003`；七对象 exact set：`bvm_storage` /
> `bvm_source_output` / `published_qb` / `bq_v4` / `standard_dcsfq` /
> `dcsfq_bvm` / `canonical_jtl`）。参数来源、reproduction/characterization
> 状态、UNKNOWN/INFERRED 项与 next discriminator 一律以该 matrix 为准。

## 指针

- **Canonical provenance registry**：`docs/research/SCIENTIFIC_RECONSTRUCTION_OBJECT_MATRIX_V1.yaml`
- **历史**：本文件 2026-08-12 之前的旧索引内容保留在 Git 历史中；本文件不再维护
  独立参数表，不并立第二 register。

## W5 边界

- **W5B**：仅在 `JH-20260813-M11B-003` audit `ACCEPTED` 后可标记完成。
- **W5A**（文献边界）：独立、开放；未完成前禁止 first / no prior work /
  literature blank confirmed 措辞。
- **W5C**（作者询问）：独立、开放；需用户授权、time-box；收到信息标
  `[AUTHOR_PROVIDED]`，不等同 `[PUBLISHED]`。

## 标签纪律（沿用）

`[PUBLISHED]` / `[AUTHOR_PROVIDED]` / `[DERIVED]` / `[INFERRED]` /
`[DESIGNED]` / `[TUNED]` / `[UNKNOWN]`；`[INFERRED]`/`[DESIGNED]`/`[TUNED]`
不得逐渐写成 paper parameter。本文件不裁决物理结论。
