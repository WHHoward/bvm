# Parallel QB→JTL interface 可视化

来源为 accepted `raw-v2` 的 M1、M2、M3、M4、M5-positive-control 和 M5-q0-scaled；没有重新执行仿真。

- 每个 `M*.html` 是一个独立 fixture；
- `interface-qb-phase-comparison.html` 对比 QB 前端；
- `interface-jtl-phase-comparison.html` 对比四颗 JTL JJ。

曲线保留 exact probe names。`P(...)` 显示为连续 `rad/2π` turns；局部 phase activity 不在这里重命名为 SFQ event。M5 historical exactly-one interpretation 已被当前 formal report supersede，图仅展示 raw trajectory；正式 verdict 以 `analysis-v2/REPORT.md` 为准。
