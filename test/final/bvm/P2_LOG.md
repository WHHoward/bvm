# P2 日志 — BVM 读写一致性验证（历史指标待重算，2026-08-09）

> [!CAUTION]
> 本文把 `P()` 的 raw rad 错当成 \(\phi/(2\pi)\)。W1/W0 的约 `+5.891/−5.886 rad` 实为约 `+0.938/−0.937` 个 JM1 相位圈，因而不能支持用该列直接证明“±6 多涡旋”或“没有单涡旋工作点”的旧证据链。读电流、重复性和 120 µA R0 擦除观察仍可保留；严格 fluxoid 数尚待完整环计算。详见 `docs/guide/project-guide.md`。

> 类型: 观察（纯测量）
>
> 网表: `test/final/bvm/test_bvm_multivortex.cir`（base，WL=±100µA）+ wl80/wl120 变体。
> 序列: W1(10-20p) R1(30-40p) W0(60-70p) R0(80-90p) W1#2(110-120p) R1#2(130-140p)，
> 固定步长 `.tran 0.1p 160p`（冻结口径，无 DST）。为在 160p 预算内加入第二次 W1/R1 循环，
> 去掉了 test_bvm_final.cir 的 HS 脉冲（HS_WL 槽位 110-120p 由 W1#2 复用）。
> 12-JJ 负载堆 (B_LD1-12, area=3.2) 与 test_bvm_final.cir 完全一致。
> 相位单位: Φ0（P() 为累积相位/2π）；settle = 脉冲结束 9-15p 后的取样值（t=29/55/79/105/129/159p）。

## 1. 写1/写0 稳态与读电流（base，WL=±100µA）

| 状态 | JM1 稳态相位 (net) | JM1 fast_events¹ | R 读电流 I(L_SL) 峰值@t |
|---|---|---|---|
| 写1 (W1) | **+5.891 SFQ**（0 → +5.891，窗口内 5 次快速事件） | 5 (W1 窗口) / 30 (全程) | R1: **+78.2µA @39.2p** |
| 写0 (W0) | **-5.886 SFQ**（+5.946 → -5.886，净 -11.83） | 13 (W0 窗口) | R0: **+17.2µA @81.7p** |

¹ fast_events = |ΔP|>0.3 SFQ/0.1p 样点数（窗口按脉冲+1p 边沿计）。JM2 全程 0 次快速事件（稳定器支路无涡旋滑移；W1 后 JM2 ≈ +0.10）。

补充观察:
- W1 后 settle 并非完全平坦: 23-29p 期间 JM1 在 5.81~5.94 间微振（≈±0.06 SFQ），R1 于 31p 开始前基本收敛于 5.89。
- R1/R0 脉冲窗口内 JM1 fast_events = 0 → 100µA 下读操作本身不触发 JM1 涡旋滑移。
- 读电流不对称: 状态 +5.9 读出 78µA，状态 -5.9 仅 17µA（详见 §4 接口意义）。

## 2. 多次循环可重复性（base）

| 循环 | JM1 settle（写后 29/129p） | I(L_SL) R1 峰值 |
|---|---|---|
| 第1次 W1→R1 | +5.8913 SFQ | +78.23µA @39.2p |
| 第2次 W1→R1 | +5.9003 SFQ | +78.70µA @139.2p |

（第 2 次 W1 是从 -5.9 状态写入，第 1 次是从 0 写入；两次 settle 差 +0.009 SFQ = 0.15%，读电流差 +0.47µA = 0.6%）

**判定: 可重复** — 两次 W1/R1 循环的稳态相位与读电流在 1% 以内一致，且不依赖写入前的初始状态（0 或 -5.9）。

## 3. 读扰动检查（base；R1 前/后取样 29p→55p，R0 79p→105p，R1#2 129p→159p）

| 读操作 | R 前 JM1 | R 后 JM1 | 相位差 |
|---|---|---|---|
| R1 | +5.8913 | +5.9460 | **+0.0546 SFQ** |
| R0 | -5.8859 | -5.9121 | **-0.0261 SFQ** |
| R1#2 | +5.9003 | +5.9449 | **+0.0446 SFQ** |

（相位差 = 读后 − 读前；drift 方向为正表示读脉冲略向 +5.9 方向推动 JM1）

**判定: 100µA 下读不破坏状态** — 漂移 ≤ 0.055 SFQ（|5.9| 的 1% 以内），R 后相位保持。
⚠️ 例外: wl120 变体的 R0 读（状态 -5.9）把 JM1 完全擦除至 ≈0（t=105p 时 +0.009，漂移 +5.90 SFQ）→ 120µA 下读"0"态破坏状态（详见 §4）。

## 4. 写驱动扫描（单涡旋工作点搜索）

| WL 幅度 | JM1 settle（W1 后） | 判定 |
|---|---|---|
| 80µA | ≈ 0（-0.021；全程 net +0.002，0 次快速事件） | **无写入**（亚阈值，JM1 不滑移） |
| 100µA | +5.89 | 多涡旋（≈6 SFQ） |
| 120µA | +5.89（W1 +5.888；W1#2 从 0 起 +5.880） | 多涡旋（≈6 SFQ），且 R0 读破坏 -5.9 态 |

**判定: 不存在 settle ≈ +1 SFQ 的单涡旋工作点** — 80→100µA 之间 JM1 从"不写"（0 SFQ）直接跳变为 ≈6 SFQ 多涡旋态，无中间单涡旋平台；120µA 仍是 ≈6 SFQ。

## 5. 结论（观察）与对接口的意义

- 多涡旋态是否稳定可重复: **是**（100µA 下两次 W1 均 settle +5.89~5.90 SFQ，±0.15%；-5.9 方向对称存在，W0 净 -11.83 可逆往返）
- 读是否破坏状态: **100µA 否**（drift ≤ 0.055 SFQ）;**120µA 读"0"态是**（R0 完全擦除 -5.9 态 → ≈0）
- 单涡旋工作点是否存在: **否**（80µA 无写入 / 100、120µA ≈6 SFQ，无 ≈1 SFQ 平台）
- 对 DCSFQ_BVM 阈值判别的影响: **读电流不随涡旋数单调、且方向强不对称** — 状态1 (+5.9) 读出 +78µA，状态0 (-5.9) 仅 +17µA；循环间读电流漂移仅 0.6%（重复性好）。这意味着:
  1. "read-1 → 恰好 1 SFQ"（最终系统门）在存储侧不成立: 存储态是 ≈6 SFQ 而非 1 SFQ，读出信号是 78µA 级脉冲而非 1 个 SFQ 脉冲；
  2. 若接口以 I(L_SL) 峰值阈值判别，只能区分"1"（≈78µA）与"0/无涡旋"。注意口径: "0 态"读数（+17.2µA @100µA WL）与"无涡旋"读数（−20.5µA @80µA WL，见 wl80 JSON peak_abs）来自**不同 WL 幅度**，直接比较有混杂；且二者**符号相反**（+ vs −）——若窗口内符号稳定，符号本身是比幅度更廉价的判别维度，值得 V4 级联时专项验证。100µA 下"无涡旋且未写入"的读数不在本次数据内（base 网表总是写入），留待 V4 补充；
  3. 判别裕度还受 WL 幅度影响: 120µA 下读"0"破坏状态，100µA 是当前最可靠工作点（非破坏、可重复、78 vs 17µA 对比 4.6×）。

## 复现信息
- 命令: build/josim-cli -o test/final/bvm/data/test_bvm_multivortex.csv test/final/bvm/test_bvm_multivortex.cir（wl80/wl120 同式）
- 指标: python3 scripts/sfq_metrics.py test/final/bvm/data/test_bvm_multivortex.csv "P(B_JM1|XBVM1),P(B_JM2|XBVM1)" --peaks "I(L_SL|XBVM1)"
- 仿真器: build/josim-cli v2.7.2837d13（冻结口径，禁 /usr/local/bin）
- 注: JSON 中 git_head = 数据生成时刻的 HEAD（4dc1789），早于 feat 提交（317fa81）属正常（先跑数据后提交）
- 确定性: 三个网表均由 run_exp.sh 双跑 md5 校验通过；base 另做独立重跑核对:
  - base 初跑 `92a956c71f20c391126cc547a15bcc3b` = 重跑 `92a956c71f20c391126cc547a15bcc3b` ✅（HANDOVER IRON RULE 5）
  - wl80 `af183f35aa0ebc97483d7215f3e83a98` / wl120 `f579b0deea12744a6a4cb676ede69ce8`
- 数据: test/final/bvm/data/{test_bvm_multivortex,test_bvm_multivortex_wl80,test_bvm_multivortex_wl120}.{csv,json}（JSON 内嵌 sha256 + git_head 交叉核对）
- 提交: `317fa81`（feat(P2) 本体；本行 SHA 由回填提交写入）

---

> **SUPERSEDED (M10, 2026-08-13)**：本文件正文与历史表格保留原样，不作改写。相位/事件计量口径已由 METRIC_SPEC_V2 v2.0.0 修正（`P(...)` 为 raw rad，圈数 = Δrad/(2π)）。P2 家族的原始 CSV 现按预注册端点算术重建为 [metrics_v2 JSON](test/final/bvm/data/metrics_v2/p2-v2.json)；中央修正表见 [HISTORICAL_METRICS_V2_CORRECTION_TABLE](../docs/research/HISTORICAL_METRICS_V2_CORRECTION_TABLE.md)。本页任何 SFQ/事件/Gate 表述仅为历史叙述，不代表 V2 口径下的物理结论。
