# PAPER-SL-Q2 可视化

`37p5u/` 与 `40u/` 各有四个 matched-case HTML 和 comparison。raw 是 Q2 accepted replay，中央 bias 与输入波形没有在绘图过程中重构。

重点是 BJs→BJL1→BJL2 的连续 phase、same-JJ voltage/current 与 OUT；phase 显示为 `rad/2π` turns。图形不把 sub-turn activity 或 voltage peak 计为 event。
