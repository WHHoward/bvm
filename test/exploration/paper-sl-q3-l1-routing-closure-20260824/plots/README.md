# PAPER-SL-Q3 L1 routing 可视化

来源为 `raw/l1-4p5/` 四个 matched cases。`comparison.html` 对照 BJs、BJL1、BJL2 与 OUT；各 case HTML 还保留 input/bias/loop currents。

该图形只展示已接受的弱 routing gain 和 zero-event controls；phase 为连续 `rad/2π` turns，不把约 sub-turn trajectory称作 event。
