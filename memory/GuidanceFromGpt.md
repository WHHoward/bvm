---
name: GuidanceFromGpt
description: 基于 memory、.remember 与仓库实测的项目状态审计和后续执行指南，供后续模型会话交接使用
metadata:
  type: project
  node_type: guidance
  date: 2026-07-17
  author: GPT
---

# JoSIM 项目状态审计与后续执行指南

> **用途**：本文件是后续模型进入项目时的优先上下文。它区分了已经验证的事实、尚待验证的假设和已被否定的路线，避免将设计计划或旧实验结论误当作已完成成果。
>
> **审计依据**：完整阅读 `memory/`、`.remember/`，并在 2026-07-17 对当前仓库重新运行 BVM、BQ、BVM→BQ 基线仿真及 `ctest`。

---

## 一、当前项目的准确定位

项目的基础设施和组件级工作已具备良好基础，但系统主线 **BVM→BQ 接口尚未成功**。

### 已验证的事实

1. JoSIM 可正常构建和运行；现有 `ctest` 的 23 个上游/基础集成测试全部通过。
2. BVM 单元、多个 ColdFlux 标准单元、SFQ 发生器均已有独立仿真记录。
3. 标准 BQ 可以在独立强驱动条件下发生约瑟夫森结相位滑移并产生输出活动。
4. BVM→标准 BQ 的直接级联没有形成可靠、离散、可被下游 JTL 接收的 SFQ 输出。
5. 低 IC BQ 版本和当前实现的 K 元件互感路线均未解决级联问题。

### 绝不能误称为已完成的事项

1. **没有可用的 BVM→BQ 接口。** 当前没有证据表明单个 BVM cell 能可靠驱动 BQ 并输出至少一个可用 SFQ。
2. **BQ 还没有被证明是“电流→可变 SFQ 数”的量化器。** 它目前更接近具有阈值和饱和行为的缓冲器/开关。
3. **BQ v4 仅是设计假设，尚未实现或仿真。** 仓库中不存在计划所列的 `circuits/qb/bq_cell_v4.cir`、独立测试和 BVM 级联测试。
4. 因此，Paper A 尚不能按“接口解决方案成功”写作；目前最多是“问题表征与候选接口探索”的阶段。

---

## 二、重新审计得到的关键数值与矛盾

以下结果来自当前仓库的重新运行，宜作为下一轮调试的基线，而不是直接与旧记录混用。

| 测试 | 当前观察 | 含义 |
|---|---:|---|
| `test/final/bvm/test_bvm_final.cir` | 运行完成 | BVM 独立测试可执行 |
| `test/final/qb/test_qb_final.cir` | BJs 累积约 96.0 SFQ | 强驱动下发生持续相位滑移；不是有限计数的充分证据 |
| `test/final/single_bvm_qb/test_bvm_bq_baseline.cir` | BJs 最终变化约 −0.00001 SFQ | 当前基线中 BJs 未形成有效翻转 |
| 同一 BVM→BQ 基线 | BJL2 最终约 0.071 SFQ | 有微弱扰动，但没有形成可用离散输出 |
| `ctest --test-dir build` | 23/23 通过 | 仿真器基础回归正常，但项目电路尚未接入回归 |

### 必须先解决的结果矛盾

旧实验记录曾描述基线 BJs 有约 1 SFQ 的连续滑移；新运行的同名基线却显示最终 BJs 接近 0 SFQ。后续模型不得忽略此矛盾。

优先排查：

1. 比较当前 netlist 与产生旧 CSV 的 commit、参数和仿真时间窗。
2. 明确使用的是最终相位、峰值相位、相位总变差，还是局部事件计数。
3. 所有 SFQ 数都统一为 `ΔP / (2π)`，并注明测量结、起止时间和符号约定。
4. 同时保存原始 CSV 与由脚本生成的指标摘要；不可只保存 HTML 图或手工观察结论。

---

## 三、对 BQ 现象的正确解释

### 1. 不要把持续相位滑移直接称作量化

独立 BQ 在强、持续或较长脉冲驱动下出现数十至数百个 SFQ 的累计相位变化，可能表示 JJ 进入持续电压态。这个数量通常依赖于驱动持续时间、阻尼、偏置和仿真窗口，而非稳定地编码输入电流幅度。

合格的“电流→可变 SFQ 数”量化器至少要满足：

1. 在定义的有限输入窗口内产生有限数量的离散脉冲；
2. 下游 Load JTL 实际接收到相同数量（或明确的固定映射）；
3. 输出数随输入幅度单调、可重复，并有无输入/低输入时的零输出；
4. 对输入宽度、偏置和器件参数扰动有可报告的工作窗口。

在这些条件满足以前，将当前 BQ 叫作“阈值缓冲器”或“候选量化器”更严谨。

### 2. BQ v4 是待验证假设，不是根因定论

v4 的核心假设是：BJL1 的低 IC 及其到地路径过早导通，吞噬 BJs 之后向输出链传播的能量；故提高 BJL1 IC、适度提高 BJL2 IC 并调整阻尼/输出电感。

这个假设合理，但提高 BJL1 IC 也可能使 DC 回流或偏置稳定性变差。实施 v4 后不能只看 OUT 电压；每个测试都必须记录：

- BJs、BJL1、BJL2 的相位和电压；
- BJL1 支路电流、输入节点电流、偏置节点电流；
- BQ 输出及 Load JTL 两个结的相位；
- 偏置建立阶段是否稳定、是否有无输入自发翻转。

---

## 四、建议执行顺序（严格优先级）

### Step 0：冻结并校准基线口径

这是所有后续工作的前置条件。

1. 选定当前 `test_bvm_bq_baseline.cir` 作为冻结基线，记录 commit hash、JoSIM 版本、`.tran` 设置和模型文件 hash。
2. 重跑独立 BVM、独立 BQ、BVM→BQ 基线各至少两次。
3. 编写一个可重复的指标提取脚本或命令，输出相位差、峰值电流和阈值事件；不要依赖人工读图。
4. 解释并修正文档中“BJs 约 1 SFQ”与当前“约 0 SFQ”的差异。

**Gate**：每个关键指标都有唯一、明确的测量定义，重复运行一致。

### Step 1：实施并独立验证 BQ v4

创建：

- `circuits/qb/bq_cell_v4.cir`
- `test/final/qb/test_bq_v4_standalone.cir`
- `test/final/single_bvm_qb/test_bvm_bq_v4.cir`

先执行独立测试，按以下顺序：

1. 无输入、仅偏置稳定性测试；
2. 标准 SFQ 注入测试；
3. 有限宽度电流脉冲扫描（例如 70/90/110/130/150 µA）；
4. 下游 Load JTL 的实际 SFQ 接收计数；
5. BJL1/BJL2 支路与偏置状态诊断。

**Gate**：v4 必须在独立测试中产生有限、可重复、被下游 JTL 接收到的输出；若仅出现持续电压态，不进入级联测试结论阶段。

### Step 2：再做 BVM→BQ v4 级联

仅在 Step 1 通过后进行。

1. 保持 BVM 与冻结基线完全一致，只替换 BQ 子电路。
2. 分别报告 BVM 读出“0”和“1”的结果，避免只展示一个成功波形。
3. 报告 BVM SL 电流、BQ 输入节点电流、三只 BQ JJ 相位和 Load JTL 相位。
4. 至少进行两次重复运行，且包含未写入/低信号对照。

**成功 Gate**：单 BVM cell 的目标读出状态稳定产生至少 1 个被 Load JTL 接收到的 SFQ；非目标状态不误触发。

### Step 3：根据 v4 结果作路线决策

| v4 结果 | 应采取的行动 |
|---|---|
| 独立测试失败 | 回到 BQ 拓扑/偏置设计，不做 BVM 级联归因 |
| 独立成功、级联失败 | 重点研究 BVM 输出波形、源阻抗、输入匹配；不应归咎于 BQ 内部 |
| 级联成功但只固定 1 SFQ | 将其定位为数字读出缓冲器，调整系统目标，不再声称可变计数 |
| 级联成功且可控多 SFQ | 进行完整量化、鲁棒性和模型扰动扫描 |
| v4 仍失败 | 停止围绕原 BQ 做简单缩放；转向专用“慢电流→受控 SFQ”接口 |

### Step 4：若需要转向专用接口

设计目标应从“让现有 BQ 被迫工作”改为“匹配 BVM 的约 30–40 ps 慢电流波形”。候选方向：

1. 过阻尼阈值 JJ / DC-to-SFQ 检测器；
2. 先整形为快 SFQ，再由 JTL/BQ 接收；
3. 重设计 BVM 输出级，降低源阻抗或提高可用电流；
4. 以输出有限脉冲数为约束的专用积分/泄放拓扑。

此前 K 元件实验只能说明**当前 K 拓扑、参数和负载下不成功**，不能推广为“互感变压器不适合所有 SFQ 脉冲接口”。

---

## 五、模型和文档需要立即收敛的地方

### JJ 模型口径

记忆中同时存在“BVM 仅 V0 (Ic×RN=0.25 mV) 可工作”和“当前 BVM 测试使用 jjmit (Ic×RN=1.6 mV) 并可工作”的描述。后续模型应建立模型兼容性矩阵，而非沿用互相矛盾的结论：

| 电路 | 模型 | 关键功能 | 结果 | netlist/commit |
|---|---|---|---|---|
| BVM | V0 | 写/读/保持 | 待统一复现 |  |
| BVM | jjmit | 写/读/保持 | 待统一复现 |  |
| BQ | jjmit | 独立输出 | 待统一复现 |  |
| BVM→BQ | 混合模型 | 级联 | 待统一复现 |  |

在这张表完成前，论文不要做“唯一工作模型”之类的绝对表述。

### 计划与记忆更新

以下旧计划内容应标注为历史假设或更新：

- 低 IC 与 K 路线的成功概率；
- “至少一条路线应成功”的 Phase 1 Gate；
- 未做鲁棒性扫描前的面积、延迟、噪声裕度预测；
- 将 BQ 直接称为可变 SFQ 数量化器的结论。

`project-todo.md` 应把 v4 明确维持为“待实现”，并在每次实验后增加一行：实验名、commit、参数、结果、下一步决策。

---

## 六、可复现性与工程改进建议

### 优先级高

1. 将 BVM、BQ、BVM→BQ 基线和 v4 的测试加入 CTest。当前 23 个 CTest 并不覆盖这些项目电路。
2. 所有论文级实验保留 CSV、netlist、版本信息和机器可读摘要；`/tmp` 仅作缓存，不应是唯一证据位置。
3. 修复硬编码绝对 include 路径，例如 `test/final/single_bvm_qb/test_bvm_paper_bq.cir` 中的 `/home/howard/JoSIM/...`，统一使用相对路径。
4. 为 SFQ 计数建立唯一的自动化输出格式，例如 CSV/JSON：`test_name, commit, junction, t_start, t_end, delta_phase_rad, sfq_count, peak_current_uA`。

### 优先级中

1. 把 HTML 图加入 `.gitignore` 或明确的 artifacts 目录；当前存在未跟踪 HTML 结果，容易使工作树状态混乱。
2. 将 `test/final` 中“参考、历史、当前基线、候选设计”分目录，避免拿旧 netlist 当正式结论。
3. 每份实验记录应注明是“观察结果”“已证实机理”还是“下一步假设”。

---

## 七、论文策略建议

当前最稳妥的论文定位不是宣称已经完成接口，而是以下两种之一：

1. **若 v4 或新接口成功**：BVM→接口的系统表征、可用工作窗口、参数扫描和通用设计准则。
2. **若接口仍未成功**：BVM→SFQ 接口失配的严格表征、现有 BQ 不适配慢读出波形的机制、已否定路线的边界条件，以及专用接口的设计要求。

无论哪种定位，论文必须有：明确的模型限制、自动化的相位计数、对照实验、鲁棒性扫描，以及不依赖单张波形图的可复现证据。

---

## 八、下一位模型的最短行动清单

1. 先读本文件、`memory/project-todo.md`、`memory/bq-v4-modification-plan.md` 与 `memory/bvm-bq-coupling-experiments.md`。
2. 不要直接写论文，也不要把 v4 当成功方案。
3. 先冻结并重现 BVM→BQ 基线，解决相位计数矛盾。
4. 实现 BQ v4，并按独立稳定性→有限脉冲扫描→Load JTL 计数的顺序验证。
5. 仅在独立 BQ 通过 Gate 后，进行 BVM→BQ v4 级联。
6. 根据 Step 3 的决策表继续，不要因旧路线图的成功概率而跳过验证。

