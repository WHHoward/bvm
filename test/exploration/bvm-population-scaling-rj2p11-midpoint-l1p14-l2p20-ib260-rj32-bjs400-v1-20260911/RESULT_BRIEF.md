# RJ2=11 midpoint — evidence-only result brief

Scientific interpretation is `NOT_PERFORMED`.

This experiment tests exactly four fresh physical solves at the frozen
RJ2=11ohm working point: masks `0001`, `0011`, `0111` and `1111`. The RJ2=12
cases are immutable comparison/reference evidence only and are never reused as
RJ2=11 physical cases.

The primary scientific source is the raw waveform for each run. The mechanical
oracle only organizes cumulative phase navigation, stored-sample voltage
clusters, ordered downstream wavefronts and terminal pulse support. No phase
turn, voltage area, current peak, L1 crossing, terminal area or response
candidate is an SFQ count.

The experiment stops after the four registered solves and ends at:
`EXPERIMENT_COMPLETE / AWAITING_SCIENTIFIC_REVIEW`.
