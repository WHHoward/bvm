# Preserved failed visualization QA

The custom summary page used display-unit aliases in its legend, so the
registered CSV labels were not present verbatim. QA failed on label identity
only. The page and QA/manifest records are retained here; no raw or metric
artifact was changed.
