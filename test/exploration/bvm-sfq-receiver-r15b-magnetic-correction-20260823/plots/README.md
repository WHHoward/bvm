# R15b magnetic-correction 可视化

来源为 R15-B 四 matched raw。曲线包含 B_DET/B_SET/B_Q/B_OUT、mutual/steering currents、DCSFQ branch 和 BVM guards。

正式 verdict 为 `ACTIVE_STAGE_NO_TRIGGER`，并保留 bounded extra back-action disposition。图形用于检查 detector activity 与 downstream stage 的分离，不把 phase range或 voltage peak当作 complete event。
