# PAPER-SL-Q5 factorial 可视化

来源为 `raw/q5-l1-4p50-l2-4p50/` 四个 matched cases。每个 HTML 与 comparison 均保留 Q5 的 exact QB probes；phase 单位为 `rad/2π` turns。

Q2/Q3/Q4/Q5 interaction 的正式数值与 `Q5_COMPLEMENTARY_DOWNSTREAM_PRESERVED_PARTIAL_L1_RECOVERY_NO_EVENT` verdict 仍以 `REPORT.md` 为准；可视化不把 `0.968... turn` 叫作 complete event。
