# RJ2 high-side second-trigger search — evidence-only result

Scientific interpretation is `NOT_PERFORMED`.

This experiment fixes L1=1.4pH, L2=2.0pH, IBias=260uA, RJ1=32ohm and the
BJS400 ARRAY history. The RJ2=10ohm pair is exact immutable reuse; RJ2=12,
14 and 16ohm are the only newly authorized values. The new values are run in
three sequential two-mask stages, and only a stage decision of `CONTINUE`
authorizes the next pair.

The final executed prefix, stage decisions, guardrail fields and any preserved
unrun decks are recorded in `qa/execution_summary.json`, `qa/stages/` and the
mechanical summary. No phase displacement, voltage area, current sign,
terminal area or threshold is reported as an SFQ count. No RJ2 threshold,
second-trigger conclusion, mechanism or follow-up is authorized here.

Final state: `EXPERIMENT_COMPLETE / AWAITING_SCIENTIFIC_REVIEW`.
