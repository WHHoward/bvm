# Adversarial review probes — mechanical only

- Canonical source raw and prior 0.3-ps raw hashes are pinned; prior raw is not copied.
- Canonical R_S/L_S3 orientation, resistor law, parallel voltage and KCL are audited before PWL generation.
- Each BVM gets its own LS3 source waveform; no common waveform.
- Run order and maximum solve count are enforced.
- The N2/N3 phase-chain oracle is separate from the historical strict voltage-cluster field; both are retained.
- No scientific winner, mechanism, physical delay or SFQ-count claim is assigned.
