# Workflow bootstrap maintenance exception — 2026-08-17

The user, as final project authority, authorizes this one-time direct
maintenance commit for the five MAINT-007 A01 working-tree changes.  It is
necessary because using the current handoff protocol to modify that protocol
again produced an avoidable scope/hash self-reference ceremony.

`JH-20260817-WORKFLOW-MAINT-007` and its C01 `REWORK_REQUIRED` verdict remain
immutable history and are not accepted by this exception.  This exception
does not authorize a future scientific L2 task, experiment, evidence change,
or workflow edit to bypass normal `josim-handoff/v1` request, ACK, receipt,
review, and audit controls.  New scientific work must use the revised normal
protocol.

The committed policy is limited to declared-entry multi-entry PRE-receipt
bundles: directory expansion is builder convenience; final receipts are
excluded; declared entries receive independent safe-path, regular-file,
SHA-256, byte-size, and duplicate-policy validation; and CRITICAL/FROZEN
completeness is defined by frozen expected evidence/run-artifact matrices and
required delivered artifacts, manifests, and inventories—not directory
closure.
