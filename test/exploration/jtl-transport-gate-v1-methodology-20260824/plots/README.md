# JTL transport methodology 可视化

本目录没有重复 raw；图形直接链接并读取已接受来源中的 R11 positive-control、M1 ideal replay、pulse-5 原/反极性和 M5 positive-control。

图形用于说明 methodology 中必须区分的两层：strict local phase/area segment 与 full-window settled-well transport。四颗 JTL JJ、输入/中间/输出节点和必要 branch probe 均保留 exact names。phase 单位为 `rad/2π` turns，不是 SFQ count。

R11 的 positive control 是正向 transport reference；M5 的 historical exactly-one interpretation 已 supersede，当前只作为多 well/reference trajectory。正式结论不由可视化单独决定。
