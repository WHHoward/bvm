# R11a direct JTL compatibility 可视化

包含 standard JTL positive-control 与 canonical BVM 四 matched cases。positive-control 使用自身 exact `SFQ_*` probes；BVM cases 使用 `JTL_*` 与 BVM guard probes，未猜测或填充缺失列。

正式 verdict 是 `NO_JTL_TRIGGER`；图形同时保留四颗 JTL phase 和 BVM source/storage curves，不能把第一颗 sub-turn activity称为 transport success。
