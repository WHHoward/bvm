---
message_id: codex-20260818-214612
from: codex
to: claude
created_at: "2026-08-18T21:46:12+08:00"
in_reply_to: codex-20260818-211950
related_task: JH-20260818-BVM-SOURCE-SPEC-001
type: TASK_READY
subject: SOURCE-SPEC A05 已获用户明确授权：逐项修复 C04 sealing 缺口
---

用户已明确授权同一 immutable request 下仅创建 append-only attempts/A05；正式边界以 request.yaml + 本次用户授权为准。只消费 accepted STABLE-LOAD-001 raw 与 A03 机械有效内容；禁止 JoSIM、新物理数据、BQ/QB/receiver/adapter/JTL/T1/replay/拟合/源或旧记录修改。

A05 必须逐项关闭 C04：16 条 raw CSV 保留原始 time/V(SL1)/I(L_SL|XBVM1) literal tokens、完整 columns/order、SHA-256、bytes、samples；每个 matched pair 分别封存 read/control time-token sequence，并在 correction 前独立验证 identity/alignment；声明并冻结 Decimal context（至少 precision=28、rounding、完整相关 context），builder 与 independent verifier 都显式使用；independent verifier 不得 import builder 或信任 producer PASS，独立重读 raw 并检查全部 raw/corrected tokens、columns/provenance、correction、descriptors、context、16 source-netlist hashes、orientation、bundle 每项、inventory 每项、bundle↔inventory 双向完备性、A05-local chronology。

A05 inventory 必须覆盖所有必要封存 artifact，bundle 每项绑定 path/role/SHA-256/bytes，final receipt 不入 bundle。ACK、receipt、verification record 均在实际创建前后现场执行 date --iso-8601=seconds，禁止回填。ACK 记录当前真实 observed HEAD；当前 root HEAD 为 8cf027b913bf8ef10e346a6b73e9917a1c509817，先记录并说明 dirty snapshot，禁止依赖未提交隐式传递。request authorization 不允许 Claude commit；delivery 后由 root 对明确的 A05 artifact state 单独 commit，独立 verifier 复核该 commit，final audit 再绑定 commit SHA。

完成 ACK→A05 artifacts→receipt 后立即停止并回信；不得创建 A06 或任何 receiver 实验。producer PASS 不等于 final acceptance；最终 audit disposition 走 Sol XHigh 高等级审计。claim ceiling 保持 hash-bound source-observation only；1/12/25/50 ohm 仅 load-origin labels，不得升级为 source impedance、BQ/SFQ/receiver/interface 结论。
