# Selected-mask population scaling — evidence-only result

Scientific interpretation is `NOT_PERFORMED`.

This experiment holds the RJ2=12ohm candidate working point and canonical
0.1ps timestep fixed. It reuses masks `0001` and `0011` and runs only the five
registered masks `0110`, `1100`, `1110`, `0111` and `1111`.

The generalized N-response oracle, control guardrails, population/position
comparison and selected-mask classification are recorded in
`mechanical_summary.json`, `qa/population_qa.json` and
`analysis/POPULATION_REVIEW.md`. Hamming weight is not used as an expected
response count, and the selected-mask result is not universal or final SFQ
proof.

Final state: `EXPERIMENT_COMPLETE / AWAITING_SCIENTIFIC_REVIEW`.
