# M11B-003 design — scope-correct scientific reconstruction matrix rework

`JH-20260813-M11B-002/A01` is preserved rework history.  Its C01 audit found
two independent blockers: the request hash-bound `REFERENCE_PROVENANCE.md`
while also requiring Claude to rewrite it, and the resulting pointer named a
historical working-tree file that does not exist.

This task fixes only those documentation and evidence-boundary defects.  It
does not re-open M11A, W5A, W5C, M9, M10, a circuit run, a candidate route, or
an interface Gate.

## Output boundary

The two output documents are deliberately **not** request inputs and do not
appear in this task's scope manifest.  They are existing draft outputs from
the committed M11B-002 rework record and may be replaced only within this
task's write scope.  All factual inputs used to support the resulting matrix
are instead hash-bound separately.

The repaired matrix must:

- retain exactly the seven requested objects;
- bind its `freeze_task` and W5B closure condition to M11B-003, not an
  unaccepted predecessor;
- use only independently hash-bound factual evidence paths—neither output
  document may serve as evidence for itself or the other output document;
- retain honest UNKNOWN/INFERRED/R0/PARTIAL_R1/current-characterization
  states; and
- remain a knowledge-state record only.

`REFERENCE_PROVENANCE.md` must be a short pointer to the matrix.  It may say
that superseded material remains in Git history, but it must not name or link
to a nonexistent repository file, create a second parameter register, or
claim W5B completion before a future ACCEPTED audit.

## No-run rule

This is a documentation rework.  No JoSIM invocation, network retrieval,
author contact, circuit/model/raw/test edit, status upgrade, candidate tuning,
or physical interpretation is authorized.  Any observation requiring one is a
recorded next discriminator for a later separately authorized task.
