# Retired reference path

The maintained topology artifact contract is now at
`.agents/skills/josim-viz/references/topology-format.md`.

This pointer remains only so historical links resolve; it is not an active
skill entrypoint.
