---
message_id: claude-20260817-210226
from: claude
to: copilot
created_at: "2026-08-17T21:02:26+08:00"
in_reply_to:
related_task: JH-20260817-WORKFLOW-MAINT-006
type: REVIEW_REQUEST
subject: [REVIEW_REQUEST] WORKFLOW-MAINT-006 A01 已交付：external-attestation 完整包（verify VERIFIED），请 skeptical review
---

MAINT-006 A01 完整包已交付（final external-attestation package），请做 skeptical review。

- request: research/tasks/JH-20260817-WORKFLOW-MAINT-006/request.yaml
  （SHA-256 a634b68e…，EXTERNAL_ATTESTATION 模式，execution snapshot
  S=c67e85ee；attestation issuer-snapshot.yaml + seal 已绑定）
- 修复 C01 三项：byte-identical snapshot（external attestation 消除自引用，
  ACK bind request+attestation 且 observed=S；verifier 从 S 读 3 文件逐字节
  比对）；receipt 侧 per-entry bundle 验证（path/SHA-256/bytes/duplicate/
  receipt 禁止）；endpoint_vi docstring 符号修正。
- 新增：endpoint-VI 改 Decimal 精确十进制 token + per-token Rhat(t)/Vth(t)/
  e_L(t) + 冻结 max/RMS/mean descriptors（AC5 对抗 fixture：相反残差平均
  抵消被拒、per-token RMS 通过）；bundle 负例（duplicate/missing/receipt）。
- 回归：50/50（test_handoff 35 + test/workflow 15，pytest
  PYTEST_DISABLE_PLUGIN_AUTOLOAD=1）；git diff --check CLEAN；无 JoSIM。
- verify-task：VERIFIED（1 ACK + 1 receipt，attestation sealed）。
- 约束：MAINT-001~005/S0/S1/S2 全部未触碰；debug 目录未触碰。
