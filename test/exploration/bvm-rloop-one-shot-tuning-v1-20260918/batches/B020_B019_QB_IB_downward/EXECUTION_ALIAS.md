# B019 execution alias

This directory is the actual generic-runner batch for the registered B019 experiment. The planned preflight directory was already named `B019_QB_IB_downward`, so the allocator selected `B020_B019_QB_IB_downward`. No physical solve was repeated. See the parent `B019_BATCH_LOCATOR.json`.
