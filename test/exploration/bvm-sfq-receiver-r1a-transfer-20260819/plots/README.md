# R1a passive pickup 路线节点可视化

来源为 accepted `l020-k080` 四 case。曲线包含 B_TRIG、L_TX/L_SEC、N_SEC、R_SEC_LOAD 和 BVM source/storage guards。

正式 disposition 是 passive extraction；图形只展示 state-dependent transfer，不把 secondary transient称为 downstream SFQ delivery。
