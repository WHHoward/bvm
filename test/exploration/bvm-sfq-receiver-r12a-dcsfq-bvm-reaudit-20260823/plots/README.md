# R12a DCSFQ_BVM re-audit 可视化

`phase-a-*` 是 0/68.4/300 µA controlled input；`phase-b-*` 是 canonical BVM cascade 四 matched cases。两组 raw 与 probe namespace 分开显示。

300 µA controlled point 的 local B3 evidence 与 canonical read1 subthreshold 必须分开阅读；图形不把 controlled positive reference外推为 BVM threshold。
